// Widget tests untuk E-Learning QC App
//
// Uncomment dan jalankan test ini dengan: flutter test
// Pastikan device atau emulator sudah running

/*
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:elearning_qc_app/main.dart';

void main() {
  testWidgets('App starts with login screen', (WidgetTester tester) async {
    await tester.pumpWidget(const MyApp());

    expect(find.text('E-Learning QC'), findsOneWidget);
    expect(find.text('Selamat Datang'), findsOneWidget);
  });

  testWidgets('App has register button', (WidgetTester tester) async {
    await tester.pumpWidget(const MyApp());

    expect(find.text('Belum punya akun? Daftar'), findsOneWidget);
  });
}
*/

// Test file di-comment untuk menghindari error saat development.
// Uncomment untuk menjalankan test.
void main() {
  // Tests will be added here
}
